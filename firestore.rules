rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // The active MoTrend web app no longer uses client Firestore directly.
    // Keep browser access closed; server/admin tooling still uses Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
